fun main(args: Array<String>) {
    val s = person.name ?: return

    loop@ for (i in 1..100) {
        // ...
    }

    loop@ for (i in 1..100) {
        for (j in 1..100) {
            if (...) break@loop
        }
    }
}